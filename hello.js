// hello world node

// Why do we need node?

/*
 It is Javascript
 Like PHP we need it for the backend using MongoDB it works pretty well to talk with our datastore, likely Firestore and Postgres
 We can use node for backend because it's faster than most other backend languages.
 Node works beautifully with express which is a node backend

*/

/*
install npm or update it

npm install -g npm
npm install npm@latest -g ( get the latest version)

*/